---
slug: /examples
description: List of react-chartjs-2 usage examples.
---

import Link from '@docusaurus/Link';
import { docs } from './docs';

# Examples

<ul>
  {docs.map(({ title, slug }, i) => (
    <li key={i}>
      <Link to={slug}>{title}</Link>
    </li>
  ))}
</ul>
